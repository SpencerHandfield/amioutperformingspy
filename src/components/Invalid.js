import React from 'react'

function Invalid() {
  return (
    <div class="alert alert-warning alert-custom fade show" role="alert">
      Please enter a valid ticker or try again
    </div>
  )
}

export default Invalid
